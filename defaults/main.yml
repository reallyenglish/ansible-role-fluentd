---
# defaults file for ansible-role-fluentd
fluentd_user: fluentd
fluentd_group: fluentd
fluentd_config_dir: "{{ __fluentd_config_dir }}"
fluentd_config_path: "{{ __fluentd_config_path }}"
fluentd_service_name: fluentd
fluentd_gem_bin: "{{ __fluentd_gem_bin }}"
fluentd_plugins_to_install: []
fluentd_certs_dir: "{{ __fluentd_certs_dir }}"
fluentd_configs: {}
fluentd_ca_key:
fluentd_ca_cert:
